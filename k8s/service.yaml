apiVersion: v1
kind: Service
metadata:
  name: main-agent-service
  namespace: agent-workshop
spec:
  selector:
    app: main-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
